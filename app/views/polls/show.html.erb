<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @poll.name %>
</p>
<% if @poll.image.attached? %>
  <p>
    <%= image_tag @poll.image.variant(resize: 100) %>
  </p>
<% end %>
<p>
  <%= link_to 'Answers', poll_answers_path(@poll) %>
</p>
<%= link_to 'Edit', edit_poll_path(@poll) %> |
<%= link_to 'Back', polls_path %>
